services:
  - type: web
    name: django-app
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn GeoExplorer.wsgi:application"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
